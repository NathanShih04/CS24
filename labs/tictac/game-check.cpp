#include "Board.h"
#include "Errors.h"
#include "Move.h"
#include <string>
#include <iostream>

using namespace std;

int main() {
    string userMove;

    while (getline(cin, userMove)) {
        try {  

        } 
        catch(const std::exception& e) {

        }
    }

    return 0;
}
